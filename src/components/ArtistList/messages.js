/*
 * ArtistList Messages
 *
 * This contains all the text for the ArtistList component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.ArtistList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Top Artists',
  },
});
