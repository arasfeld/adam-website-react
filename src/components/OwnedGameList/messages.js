/*
 * OwnedGameList Messages
 *
 * This contains all the text for the OwnedGameList component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.OwnedGameList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Owned Games',
  },
});
