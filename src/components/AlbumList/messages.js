/*
 * AlbumList Messages
 *
 * This contains all the text for the AlbumList component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.AlbumList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Top Albums',
  },
});
