/*
 * TrackList Messages
 *
 * This contains all the text for the TrackList component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.TrackList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Recent Tracks',
  },
});
