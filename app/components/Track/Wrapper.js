import styled from 'styled-components';

const Wrapper = styled.div`
  align-items: center;
  display: flex;
  font-size: 0.8em;
  padding-bottom: 8px;
  padding-top: 8px;
`;

export default Wrapper;
