/*
 * Track Messages
 *
 * This contains all the text for the Track component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.Track';

export default defineMessages({
  playing: {
    id: `${scope}.playing`,
    defaultMessage: 'Now Playing',
  },
});
